const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={globalThis:null};c.globalThis=c;vm.runInNewContext(fs.readFileSync('data.js','utf8'),c);const d=c.TRAVEL_DATASETS;assert(d&&d.ITINERARY_DATA&&d.BOOKINGS_DATA);assert.equal(Object.keys(d.ITINERARY_DATA).length,2);assert.equal(d.ITINERARY_DATA['2'].status,'open');assert.equal(d.BOOKINGS_DATA['fixture-hotel-booking'].type,'accommodation');console.log('FIXTURE DATA: PASS');
