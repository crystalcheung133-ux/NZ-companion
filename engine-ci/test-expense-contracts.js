const fs=require('fs'),assert=require('assert');const e=fs.readFileSync('runtime/expenses.js','utf8');for(const x of ['expenseSaveInFlight','customSharesForSave','depositAmount','depositCurrency','localCommitComplete','sourceBookingId','openBookingExpense','getBookingExpenseLinks'])assert(e.includes(x),`missing ${x}`);const w=e.indexOf('writeExpenses(arr);'),c=e.indexOf('closeExpenseModal();',w),r=e.indexOf('window.renderExpenses(operation)',w);assert(w>=0&&c>w&&r>c,'commit ordering invalid');console.log('EXPENSE SAFETY: PASS');
