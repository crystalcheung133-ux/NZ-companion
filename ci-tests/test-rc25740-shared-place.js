const fs=require('fs'),vm=require('vm'),assert=require('assert');
const mem={};const ctx={console,globalThis:null,STORAGE:{local:{readJSON:(k,d)=>k in mem?JSON.parse(mem[k]):d,writeJSON:(k,v)=>(mem[k]=JSON.stringify(v),true)}},GenerationSelectionAdapter:{view:()=>({places:{p1:{title:'Old',address:'Old address',phone:'1',website:'old',maps:'oldmap'}}})},document:{dispatchEvent:()=>{}},CustomEvent:function(){}};ctx.globalThis=ctx;vm.createContext(ctx);vm.runInContext(fs.readFileSync('place-authority.js','utf8'),ctx);
assert.equal(ctx.PLACE_AUTHORITY.get('p1').address,'Old address');let r=ctx.PLACE_AUTHORITY.save('p1',{address:'New address',phone:'2',website:'new'});assert(r.ok);assert.equal(ctx.PLACE_AUTHORITY.get('p1').address,'New address');assert(ctx.PLACE_AUTHORITY.mapURL('p1').includes('New%20address')||ctx.PLACE_AUTHORITY.mapURL('p1')==='oldmap');
const guide=fs.readFileSync('guide-runtime.js','utf8'),day=fs.readFileSync('day.html','utf8'),trip=fs.readFileSync('trip-runtime.js','utf8');
assert(guide.includes('PLACE_AUTHORITY.get(key)'),'Guide does not resolve shared Place authority');assert(guide.includes('saveGuidePlaceEdit'),'Guide Studio writer missing');assert(day.includes('PLACE_AUTHORITY.get(item.placeId)'),'Timeline does not resolve shared Place authority');assert(trip.includes('PLACE_AUTHORITY.save(next.placeId'),'Booking does not write shared Place authority');console.log('RC25.7.40 SHARED PLACE CONTRACT: PASS');
