# RC24.1 — UX Contract CI Path Fix

Fixed the UX contract test so repository files are resolved from the test file location, independent of the process working directory. No runtime UX behaviour changed.
